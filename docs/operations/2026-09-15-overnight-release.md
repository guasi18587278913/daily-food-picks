# 9月15日夜间收尾与体验版

用户授权助手负责夜间调试、部署及自动采集核对，睡醒后只进入小程序验收。体验版0.1.3已上传并选中，云端代码源为111403b6a905e44618f469d2de37f17fbae4e156。06:00结果另见后续记录，不提前当作成功。

## 当前实现

- 卡片、封面及标题进入“查看内容”页。正文、作者、发布时间、采集时间及已有图片/视频来自已保存数据，标明“小红书 · 已采集内容”。这不是小红书原生页面，也不宣称解除平台外跳限制。未恢复来源复制。
- getContent先验证当前微信身份和已发布索引，再核对原候选的轮次、笔记ID、作者及类型。只返回所需正文/媒体字段，不返回内部判断、密钥或原始接口响应。读页面不触发TikHub、模型或数据库写入。
- 新采集保留最多20张已取得配图及完整性标记；旧数据缺图、正文未知和媒体加载失败均明确显示，不编造内容。
- 老记录缺少封面来源字段时，读取同篇已完成候选获取原图回退；云封面失效时仍可尝试来源图片。
- 正文模型连续两次失败后，本轮暂停新的正文调用。合格视频在既有视觉20次/轮、0.50元/日范围内继续复核，错误证据保留；图文及未能复核内容保持未完成。有可靠结果才发布partial，全无可靠结果保留旧快照。
- 正文调用预留、候选状态与失败计数原子保存；中断恢复不重放未知正文调用，不重复计数。本地不完整正文不会消耗模型次数或重置失败计数。
- 封面失败新增安全诊断，区分下载、HTTP、格式、大小、上传与缓存问题。上传成功但缓存写入失败仍保留可用fileId。

## 已完成验证

1. 全部小程序模块测试：`npm test`，327通过/0失败/0跳过，退出0。前端类型检查退出0；严格部署包检查12通过，客户端23文件/88207字节，Linux解码器7918000字节。
2. 模型故障回归先复现3失败，再完成修复。独立审查发现并修复本地短路打断连续失败、中断导致失败计数滞后两项；独立恢复与脱敏补验7通过。模型/状态恢复审查无遗留阻塞。
3. 内容读取、权限、媒体字段和前端独立审查发现并修复旧云封面无原图回退、网络失败错误提示和未知正文误称无正文3项；复审通过。
4. 真实只读赠额模型探活2次：hunyuan-v3/hy3简单输入约984ms返回；昨晚失败的同一候选约1065ms返回uncertain，使用379 token。官方文档确认hunyuan-v3只消耗赠额、额度不足报错，不切套餐模型。昨晚实际异常原因仍因缺日志无法复原。
5. 3条已发布视频各请求前64KiB：均HTTP206、video/mp4、文件头有效。随后在微信开发者工具中均实际播放超过2秒，时长分别约91.72/74.44/80.71秒，无video错误。竖屏播放器按真实尺寸加高，实际358×520px。
6. 模拟器展示图文布局样例2张图片，并验证首页3个查看按钮→详情→返回列表。该阶段云接口使用本地替身、视频URL为真实已发布内容，不能当作云端权限或手机验收。首次仅等待500ms即检查路由过早，等待导航完成后通过，未据此修改业务逻辑。

## 已执行封面补存

仅对19:17已发布的`6aa39f6d000000002802d430`、`6aa53dc00000000019030164`补图。执行前验证已发布快照/索引/原候选、轮次、笔记与作者、video类型，备份原记录。每张最大2MiB，沿用既有云存储路径；上传后事务比较原候选及发布引用，只补候选的fileId，不修改历史快照或推荐顺序。

只读预览及独立审查通过后执行：2成功/0失败，命令退出0，没有封面异常诊断。3条当前选题均能取得云封面临时链接。原始记录与执行日志位于忽略目录`.local/overnight-release/`。

## 线上与调度基线

独立只读审查：旧collectTick部署为bad4f6d，24个受控文件与远端一致；定时器及口令正常，无活动轮次/临时补跑/视觉停用标记。

9月14日20:00轮因首次正文MODEL_UNAVAILABLE失败，TikHub仅3次/0.03美元、文字调用1次、视觉0次、处理完0个候选，未发布。页面最新仍为19:17的3篇（今日0/本周2/黑马1）。不得把它说成额度耗尽或已完成更新。

9月15日既有研究4次/0.04美元已计入250次日总额。06:00最多100次，09/12/20常规各最多50次，仍受当日总额约束；视觉20次/轮、0.50元/日不变。本次封面与内容读取验证没有新增TikHub或视觉模型调用。

## 部署与待验收

| 项目 | 实际结果 |
| --- | --- |
| catalog更新 | Active，11个文件与固定源码逐项一致，0差异；zipSHA a6cbc738bf1244ca951fad07187e08e4ef88b8f172844122cb994848306282da |
| collectTick更新 | Active，26个文件与固定源码逐项一致，0差异；ffmpeg/ffprobe执行位保留；zipSHA 85e80f5d4ae735cc78d5fee51ca8c21e3e5b99de712bc055019bb08596cd86c3 |
| 关键配置 | 环境变量、运行时、入口、内存、超时、权限、层、网络及触发器对应字段均保持；未改预算配置 |
| 零收费探针 | catalog缺身份拒绝；collectTick错误口令拒绝、正确口令窗口外返回outside_window；TikHub/视觉日账和最新指针均未变化 |
| 真实权限读取 | 微信开发者工具以实际登录身份读取生产，角色admin；3条正文分别121/141/397字符，完整性true；视频URL与已实际播放的来源完全一致，封面均有地址 |
| 未发布内容 | 实际登录身份请求不存在的笔记，返回NOT_FOUND，无data |
| 体验版 | 微信CLI上传退出0，编译包92164字节；后台回读0.1.3、02:41:48提交、已标记体验版，入口pages/index/index；未提交公开审核 |

部署执行器经独立审查修正目标配置固定、云端代码漂移、15分钟操作裕量、两种日账、最新指针证据范围后执行。备份zip哈希及可读性已验证，代码回退步骤在私有ROLLBACK.md，不回滚账本/快照。`snapshotUnchanged`未被用来冒充完整快照检查，本次核对的是最新指针。

后台正确体验二维码已保存在私有目录`experience-0.1.3.png`，页面显示9月22日前有效；它不是前面的外跳调试码。

06:00正式轮结果待核对，已启动只读观察器等待9月15日06:00，最迟06:40结束并记录轮次/预算/候选。小红书外跳和同篇入口映射仍未解决，来源控制样本不进入榜单。手机播放、图片预览、收藏和重新进入由用户睡醒后验收，不提前标为通过。

### 后续真机反馈与作者主页

用户已反馈当前作品点入后能看到视频及对应图文；这是内容可见性的真实用户反馈，不扩张为收藏、重新进入或全部媒体异常场景已验收。用户发现作者名称无法点击，要求进入该作者的小红书主页；当前0.1.3确实未实现作者点击。

用户提供另一作者“脑袋空空不想动”的原生小程序主页截图及官方短链。目标页面存在，但还没有当前美食作者的可靠入口映射。现有get_user_info归档仅发现网页分享链接，不能冒充微信小程序路径。该样本不写入选题榜或生产绑定。

桌面微信已找到用户发送的样本链接，尚未成功打开。连续窗口捕获失败后重新检查系统边界，04:07确认系统会话锁定（CGSSessionScreenIsLocked=1）；捕获授权本身为true。停止重复尝试，不猜测路径或绕过锁屏。需正常解锁后继续官方入口核验。已授权的06:00只读观察器仍在运行；本轮作者调查没有新增TikHub/视觉调用、未向他人发送消息。

### 前两天轮次回查

9月15日03:42只读回查云端轮次与日账。下表金额为调用账本按既定单价记录，并非钱包扣款对账；文字调用历史计数可能包含旧代码的本地短路，不作为真实模型请求数。

| 日期/轮次 | TikHub次数 / 美元 | 发布新增（今日/本周/黑马） | 实际状态及原因 |
| --- | --- | --- | --- |
| 9/13 09:00 | 17 / 0.17 | 0/0/1 | partial，调用额度耗尽 |
| 9/13 12:00 | 14 / 0.14 | 0/0/2 | partial，内容判断不可用 |
| 9/13 20:00 | 4 / 0.04 | 未发布 | failed，部分数据请求失败 |
| 9/14 06:00 | 68 / 0.68 | 未发布 | failed，部分数据请求失败 |
| 9/14 09:00 | 17 / 0.17 | 未发布 | budget_exhausted，候选未覆盖完、部分请求失败 |
| 9/14 12:00 | 17 / 0.17 | 未发布 | budget_exhausted |
| 9/14 13:31补跑 | 20 / 0.20 | 0/0/0 | partial，发布已扫描零新增状态 |
| 9/14 19:17补跑 | 18 / 0.18 | 0/2/1 | partial，视觉3次，存在画面读取/复核缺口 |
| 9/14 20:00 | 3 / 0.03 | 未发布 | failed，首个正文判断不可用 |

日账总计：9/13为35次/0.35美元，视觉0；9/14为167次/1.67美元（已含研究12次，不重复相加），视觉12次、已知及保留金额均为0.019566元。日账含验证/研究，不能用表内正式轮次之和替代全天总额。原始只读结果留在私有目录。

## 云资源用量核对

CLI的usage/info仅支持资源点套餐，本环境是非资源点个人版，虽退出0但提示不支持，未把它当成功读取。改用已有CloudBase控制台刷新后核对：本周期截止昨日调用约1.09万/20万，本日显示324；存储流量本日946.34KB、CDN回源201.74KB；计算本日0.55CU/15万CU。页面各项显示“额度正常”，按量付费开关仍开启。数据有统计延迟，不等于完整实时账单；未升级、续费或修改付费开关。

# 视觉每轮20次上线记录

2026-09-14 19:44（上海时间），按用户明确要求“视觉提高到每轮最多20次”，已部署源码 `bad4f6d9919fade43d5ea7f9b708bd0f009e86be`，配置仅将 `DFP_VISION_ROUND_CALLS` 从3改为20。

- 后续新轮次最多20次视觉请求，每日仍封顶0.50元，达到任一上限停止。
- TikHub仍为日250次，06:00为100，常规各50次。模型、内容规则、定时器均不变。
- 已创建的19:17旧轮次保留原3次定义；旧首验和追加实验账未重开。
- 20:00新轮次使用20次上限，仍受采集窗口和可用素材约束，不保证每轮完成20篇复核。

相关26项测试通过、0失败，退出码0；覆盖并发21个预留最多20、旧轮仍3、日金额拦截、首验不扩大、非法21次配置拒绝。独立审查通过，严格部署包检查12项通过。

云端回读：Active、Nodejs20.19、512MiB、180秒；26个文件逐项摘要一致、0差异。主目录私有配置同步。触发器、日预算记账、旧快照均未变化。部署包SHA256为 `eeff0bc3c8baa328c3c3c8df0b07b21910ca254913de73508277325f1af1ffa0`。

未授权请求返回UNAUTHORIZED_TRIGGER，合法窗口外请求返回outside_window。探针前后TikHub及视觉日账均未增加，没有额外调用收费模型。部署备份和回读证据保存在忽略目录 `.local/vision20-rollout/`。

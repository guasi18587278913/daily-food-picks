# 微信指定成员试用版运行记录

业务记录日期：2026-09-12，北京时间。2026-09-13 仅整理位置与账号脱敏；源码、规格和规范已统一在本项目，仍沿用 feature/wechat-member-trial 分支。

## 当前结果

微信体验版 **0.1.1** 已上传并设为体验版，后台已添加大芙。云端已完成一次真实定时验收和当天 20:00 的正式更新，合计保留 3 篇制作类选题；原网页及旧轮次文件未改。

尚未完成大芙的首次应用身份绑定和真机六项验收。已给出体验码，请她打开一次；首次可能显示“当前账号尚未开通试用”。这时后台只记录微信提供的真实身份，由维护者核对后加入唯一试用者名单，不会自动授权陌生人。

## 授权与费用

用户于 2026-09-12 明确选择“按方案开始，每天最多 50 次”。首次真实验证最多 20 次、0.20 美元；每个北京时间自然日最多 50 次、0.50 美元，包含首次验证和所有重试。此上限用于 TikHub 数据接口，不是用户浏览次数。

当日程序账本为 **36 次、0.36 美元**：首轮验收 20 次，20:00 正式轮 16 次；36 次请求均成功，失败和未知均为 0。金额按已核验的每次 0.01 美元计提，未把程序账本当作供应商发票；没有为补图和历史更正重新调用收费数据接口。

模型固定使用成长计划赠额通道 hunyuan-v3 / hy3，不切换付费模型。每次最多输出 1024 Token，35 秒超时、无工具调用、每轮最多 20 次判断。两轮业务及一次正文复核共记录 27 次判断尝试；部署时的探活与这些业务计数分开看。

腾讯云超出套餐按量付费是用户本人开启的选项，已在控制台回读确认。数据接口上限不是腾讯云账单硬上限。本任务没有购买、充值或升配；不把尚未核对的云资源账单写成零费用。

## 账号与资源

- 名称：深夜食堂选题台；AppID：wx8a2388888683b769。
- 主体：个人；类目：工具 / 信息查询，已通过并设主营。
- 腾讯云专属账号标识保留在本机私有记录；环境：food-picks-trial-d5elis0ecfcb5d2，上海地域。
- 成长计划报名和环境领取成功，实际套餐为个人版；控制台到期日为 **2027-03-12 23:59:59**。
- 控制台返回 IsAutoRenew=true、EnableOverrun=true；记录现状，不代表 Agent 额外设置了自动续费或充值。
- 2026-09-12 18:02 回读成长计划：赠送生文配额 1,000,000,000 Token，使用前剩余 100%；另显示赠送生图 100,000 张，本业务不调用生图。此数值是当时观测，后续已有模型用量。
- 官方说明赠额有效期为 6 个月；本次控制台没有显示单独的模型到期时刻，不把推算日期冒充实测。
- 尚未备案、未微信认证；已实测允许上传并设置体验版。未提交正式上线审核。
- 未生成 AppSecret 或代码上传私钥；凭据只在本机私有配置或云端环境中，不写入本记录。

## 实际部署

| 云函数 | 运行时 | 超时 | 内存 | 状态 |
| --- | --- | --- | --- | --- |
| collectTick | Nodejs20.19 | 180 秒 | 512 MiB | Active，采集已启用 |
| catalog | Nodejs20.19 | 30 秒 | 256 MiB | Active，只读接口 |

9 个业务集合与文件存储均已应用 ADMINONLY 权限，普通小程序客户端不能直接读写。catalog 在核验微信真实 AppID/OpenID 后，才读取数据并签发返回作品的临时图片链接。

读取身份只使用云函数本次第二参数，经parseContext解析；不从process.env、getWXContext、getCloudbaseContext或客户端event继承身份。缺失、格式异常和别名冲突均拒绝。

维护验收账号已授权。大芙的平台体验成员已添加，开发版权限未开放；应用身份仍等待首次打开。临时 DFP_CAPTURE_CALLER_FOR_SETUP 只捕捉未授权的真实同 AppID 调用者，绑定完成即关闭；不得从请求参数直接认定 OpenID。

正式定时器 food-picks-timer 已启用，表达式为 `0 0-20/2 9,12,20 * * * *`，时区为北京时间。每天 09/12/20 点开始，前 20 分钟每 2 分钟续跑一次，第 20 分钟只收尾。19:08 的临时验收时刻已移除；日预算分配为 17/17/16。

每次最多 5 个外部数据请求，当前串行执行；单请求 40 秒，150 秒停止领取新工作，180 秒云函数超时。全局租约有效期 210 秒，失败请求和未知结果不退还已预留额度。

原生腾讯云定时器的实际 SOURCE 为空，不能只依赖 wx_trigger。已用独立无收费探针定位，再改为核验仅保存在服务端配置和定时器 CustomArgument 中的随机口令；仍拒绝微信用户上下文和伪造事件。诊断用函数与触发器已删除。不要在日志、命令行参数或截图中输出该口令。

## 两轮真实数据

| 轮次 | 数据请求 | 结果 | 保留选题 |
| --- | --- | --- | --- |
| 9 月 12 日 19:08 验收 | 20 次 / $0.20 | 部分覆盖 | 本周热门 2 篇 |
| 9 月 12 日 20:00 正式 | 16 次 / $0.16 | 部分覆盖，20:06 完成 | 低粉黑马 1 篇 |

首轮搜索“美食教程 / 家常菜”，处理 15 个候选；正式轮搜索“一人食 / 创意料理”，处理 11 个候选。每轮两个词各查图文和视频一页，预算用尽后明确展示部分覆盖；不保证每天都有今日新锐，也不把旧数据改成今天。

当前保留：椰壳是巧克力的，椰冻是微醺的（16,876 赞）；特浓抹茶无花果蛋糕（13,284 赞）；有幸在亲戚家吃过一次，被惊艳到了（858 赞，作者 4,903 粉）。它们分别有制作动作或具体食材配方证据。

时间与门槛：今日新锐为近 24 小时至少 1,000 赞；本周热门为近 7 天至少 10,000 赞；低粉黑马为近 5 天至少 300 赞且作者粉丝不超过 5,000。作者历史基准严格取前 7 篇、中位数第 4 个；今日倍率与黑马赞粉比分开，缺失数据不制造数值。

已对首轮 10 篇代表性判断作内容抽核，并核对最终 3 篇的正文、指标和日期。椰冻与蛋糕的真实小红书原文页已读取，标题、作品编号及制作内容相符；这不等于已验证手机能从微信自动拉起小红书。

首轮曾误收 35 赞黑马，已按原有 300 赞门槛更正。蛋糕因证据输出不连续被标不确定，使用已保存正文增加 1 次免费模型复核后收录。旧快照隐藏但保留审计，新快照与引用在一个事务中切换；20:00 最新轮次和 36/20 次账本未改。

更正时发现 CloudBase 不接受同一事务内并行读取。只读对照验证：7 条并行读取失败，7 条顺序读取成功。管理脚本改为顺序执行后提交成功；正式业务事务原本已经顺序执行。

封面源站会返回 application/octet-stream。已发布修复：只在文件字节识别为 JPEG / PNG / WebP 时缓存，保留域名限制、HTTPS、禁止重定向、10 秒和 2 MiB 上限。两张历史封面已补齐，全部3篇均有封面。新快照为20260912-1908-38debb46d33a和20260912-2000-3b83ab6a7f2f；正文、指标、顺序及取数结束时间未变。蛋糕图与真实原文页来源一致，黑马图与原候选图片地址字节一致，独立目视及摘要核验通过。

## 验证证据

| 源码状态 / 验证 | 实际结果 | 退出码 |
| --- | --- | --- |
| 9ca34ae，Node 20.19.0 初始全部业务测试 | 49 通过，0 失败 | 0 |
| 844d064，定时来源及受影响模块 | 29 通过，0 失败 | 0 |
| c1de62c，采集解析及流程 | 18 通过，0 失败；候选顺序后补 6 通过，0 失败 | 0 |
| c1de62c，读取权限及搜索 | 5 通过，0 失败 | 0 |
| c1de62c，前端类型检查 | 0 错误 | 0 |
| c5a8f7a，封面与快照发布模块 | 7 通过，0 失败 | 0 |
| 6d8c605，当前请求身份、权限和预算 | 25 通过，0 失败 | 0 |
| 6d8c605，部署包及复制一致性 | 6 通过，0 失败 | 0 |
| 6d8c605，云端伪造身份与伪造定时调用 | 两项均拒绝；未授权读取返回UNAUTHENTICATED | 0 |
| 正式时刻表 | 66 通过，0 失败 | 0 |
| 初次客户端包检查 | 6 通过，0 失败 | 0 |
| 微信预览编译与开发版上传 | 0.1.0、0.1.1 均实际成功；0.1.1 已设体验版 | 0 |
| 封面修复部署 | collectTick 单独部署成功，业务成功信息 1 条 | 0 |
| 历史纠正提交与回读 | 新快照 2 篇；最新轮次、状态、日账与初验账均保留 | 0 |
| 两轮历史封面补齐与回读 | 3 篇 / 3 封面；费用与状态不变 | 0 |
| 已部署采集函数伪造定时调用 | 返回 UNAUTHORIZED_TRIGGER，拒绝执行 | 0 |

独立审查覆盖初始实现、原生定时身份校验、内容门槛与候选排序、封面格式、历史纠正和补图脚本。当前请求身份修复6d8c605也已独立复审并部署两函数成功。发现问题后修复并复审，没有以 CLI 退出码为 0 替代平台业务结果；首次 Creating 状态下的失败部署已被后续成功部署替代。

微信开发工具的真实页面已自动读到 20:00 轮次，并成功跨历史搜索“椰”，返回原文对应作品和封面。往期列表可见两轮。此证据来自模拟器，不能替代大芙手机验收。6d8c605部署后电脑处于锁屏，修复后的真实微信页面回归仍待解锁或大芙手机打开。旧版管理员CLI调用曾返回有权数据，排查后按官方实例复用说明修复了身份读取。它仍不能替代普通未授权微信账号的真机检查；指定成员绑定与真机核验尚待完成。

## 停止、恢复与交付操作

停止采集：先将私有配置 DFP_ENABLED 改为 false 并部署 collectTick，再删除 food-picks-timer。保留所有已发布数据、纠正记录和预算，不删除数据库，不清账补量。仅停止采集不会撤销用户自行开启的云资源超额策略。

恢复前核对 TikHub 价格、赠额余额、未决请求和当日剩余额度；原价不能验证时保持停用。确认模型仍为 hunyuan-v3 / hy3，无付费回退，再用同一个定时器和已有轮次进度恢复。正式定时器的服务端口令必须与云函数私有环境一致，不能通过客户端设置。

大芙首次打开后：读取本次真实调用者，核对时间和 AppID，只把该 OpenID 加入允许名单；保留必要维护者，关闭临时捕捉。请她重开验证进入、新轮次、搜索、排序、收藏重开及复制来源。没有真实完成就保留待验收，不把模拟器结果替代真机。

前三天每日记录数据请求与费用、三个板块新增数、未完成原因、判断漏收/误收、来源链接和图片可用性、云资源用量。后续仍需观察两个正式轮次，尤其是次日 09:00 和 12:00；不额外造轮次或清预算做重复收费验证。

实例复用会保留进程变量，所以用户身份必须绑定本次请求。已用真实SDK本地复现旧身份残留，并在两入口回归；部署后管理员伪造身份调用已从旧版ok改为明确拒绝。[CloudBase官方实例复用说明](https://docs.cloudbase.net/cloud-function/instance)

官方依据：[TikHub 当前小红书单次价格](https://tikhub.io/xiaohongshu-api)、[成长计划模型使用指南](https://docs.cloudbase.net/ai/ai-inspire-plan-guide)、[成长计划规则与期限](https://docs.cloudbase.net/ai/ai-inspire-plan-rules)、[微信定时触发器](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/guide/functions/triggers.html)。

## 20:50 零取数兼容性检查

检查前确认活动轮次已为partial、验收时间配置已清空，再安排一次位于采集窗口外的原生Timer。触发时刻过后立即恢复正式三轮表达式，并回读服务端口令一致。数据日账仍36、初验20，业务模型判断仍27，已发布两轮和取数结束时间不变。

旧版函数日志接口已不支持查询；新CLS只读查询返回LOG_SERVICE_NOT_ENABLED，环境未开通该日志服务，本次未开通。没有取得此次空跑的业务返回值，因此不把空日志或零新增数据直接当作outside_window已验证。正式三轮已恢复。

通过官方1分钟监控补充核验：20:50为1次collectTick执行、平台错误0，查询退出码0。此指标能确认调用发生，但不能代替业务返回值；20:47的被拒绝手动调用同样被平台统计为无错误。

## 2026-09-13 22:07 今日新锐 06:00 扫描部署

用户于 22:04 确认“提交并现在部署”。源码为分支 `feature/today-hot-sweep` 的提交 `2afbedb`（基于 main `6943e3a`，未合并 main）；需求见 001 spec 的 FR-025/FR-026，任务见 tasks Phase 8。

授权额度：每天最多 150 次、1.50 美元（含重试）；06:00 扫描轮最多 100 次，09/12/20 仍为 17/17/16。

部署前核对：TikHub 公开价格页仍为每次 0.01 美元；线上 collectTick 没有残留验收时刻，环境变量与私有配置一致。TikHub 余额交由用户在后台确认，未调用接口查询；云资源用量未核对。

执行与回读：私有配置改为 `DFP_DAILY_CALLS=150`、`DFP_DAILY_MICRO_USD=1500000`、`DFP_SWEEP_CALLS=100`，写入前已用新代码的配置校验。仅从工作树部署 collectTick，22:07:36 状态为 Active（Nodejs20.19、180 秒、512 MiB）；回读线上额度一致、验收时刻为空。不带口令的伪造定时调用返回 UNAUTHORIZED_TRIGGER，未产生采集请求。food-picks-timer 回读为 `0 0-30/2 6,9,12,20 * * * *`，服务端口令一致。catalog 未改动。

同晚发现：20:00 轮 4 次搜索全部为 HTTP_REJECTED（HTTP 4xx，且不是 401/403/429），该轮失败，当日程序账本 35 次；当天 09:00、12:00 两轮正常。程序未记录具体状态码，原因未查明，需先在 TikHub 后台核对余额和密钥状态。若 06:00 扫描仍被拒，该轮失败、不产生选题，被拒请求仍占用当日程序额度。

待观察：9 月 14 日 06:00 首次扫描的请求数、今日候选数、入选数和费用；09:00 是否带上 06:00 的结果。

协调与回退：主目录的微信登录任务仍有未提交改动，日后部署 collectTick 前必须先合并本分支，否则会覆盖扫描功能。主目录私有配置已是新额度，旧代码读取会拒绝运行，不会超支但会停止采集。回退时清空 `DFP_SWEEP_CALLS`、把日额度改回 50 / 500000 并重新部署配置即可；定时器可以保留新表达式（没有 06:00 额度时不建轮），或改回 `0 0-20/2 9,12,20 * * * *`。

## 2026-09-14 首次 06:00 扫描观察（09:58 只读核对）

核对前 CloudBase CLI 提示登录失效，执行 `tcb login` 检查后凭据恢复可用；全程只读，未发起采集或收费请求。

06:00 轮：06:00:04 开始、06:12:40 结束，状态 failed，未发布。请求 68 次，其中搜索 55 次（54 次成功、1 次 HTTP_REJECTED，关键词“月子餐食谱”），细查 13 次；程序账本 0.68 美元；内容判断 13 次，使用赠额模型。1080 条搜索结果中，24 小时内至少 1000 赞的有 13 条（去重），全部未通过内容判断：not_cooking 10 条、uncertain 3 条，入选 0 篇。入选为零又有 1 次搜索失败，按既有规则记为失败、不发布。13 条里约一半看起来是做菜视频，但正文只有一句话或只有标题（如“电饭锅一锅出，三菜一汤一主食”“清炖牛腱肉➕卤牛腱”），属于规格已知的“步骤只在视频里”的限制；其余是旅行、宠物、晒晚餐等非制作内容。

09:00 轮：状态 budget_exhausted，未发布。请求 17 次（搜索 4、细查 8、粉丝查询 5，其中 1 次 NETWORK_ERROR 结果未知），0.17 美元。45 个候选截取 40 个，只处理到第 7 个；6 篇判为制作内容，其中 4 篇作者粉丝超过 5000，1 篇粉丝查询结果未知，1 篇在额度用完前还没查粉丝；入选 0 篇。

“带上”验证：06:00 没有已发布快照，09:00 本身也未发布，线上“带上”逻辑尚未被实际触发。页面最新结果仍是 2026-09-13 12:00 轮。当日程序账本 85 次、0.85 美元。06:00 的 55 次搜索只有 1 次被拒，前一晚 20:00 整轮被拒的情况没有再出现。

## 2026-09-14 15:55：新增研究授权与全日统计口径

用户在第一轮路径实测后明确批准继续付费验证。原 150 次预算池中已有 134 次、晚间保留 16 次，本次另行授权最多 12 次／0.12 美元的一次性研究；两部分全天合计上限为 162 次／1.62 美元，自动采集配置不变。

本次已用 12 次、均成功；实验已关闭，结果没有发布。当天原池仍为 134 次／1.34 美元，额外研究为 12 次／0.12 美元，**实际合计 146 次／1.46 美元**，剩余 16 次正常采集额度。

读日账时必须将 `calls + additionalResearchCalls`、`microUsd + additionalResearchMicroUsd` 分别相加；只读原字段会漏报本次消费。此前补跑和第一轮研究已经包含在原字段中，不要重复累计。完整证据与候选结果见[公开收藏与热点灵感实测](2026-09-14-discovery-followup.md)。正式 20:00 轮观察时沿用此总额口径；本次不替代 T042/T048 的定时验收。

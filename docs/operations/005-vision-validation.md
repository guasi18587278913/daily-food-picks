# 005：自动视频复核首验（2026-09-14）

## 当前结论

已经打通真实视频抽帧→VITA视觉请求→结构化证据→独立预算结算。**用户追加授权后的V3三例验证通过：两段漏收教程通过、吃播排除，SC-002已满足本次样本验收。** 六次首验与追加三次分账保留，不将小样本结果等同长期准确率。下面保留V1/V2失败和修订记录。

本次使用图像识别直接接口 `vita-video-3.0`，实际支持JPEG Base64。响应中的model、finish_reason、usage均参与验证。每次请求 `max_tokens=16384`（输入输出合计）、`max_completion_tokens=1024`。公开输入价1.2元/百万tokens、输出价3.5元/百万tokens；调用前保守预留0.023245元，合法usage后向上取整到微元结算。未发生未知费用或账本释放异常。表中费用按返回usage与核验单价计算并写入应用预算，尚未另行核对腾讯云出账明细。

## 六次实际结果（全部保留，不挑选有利输出）

| 次序 | 提示词 | 样本 | 模型结论／程序最终结论 | 实际人民币 |
| --- | --- | --- | --- | ---: |
| 1 | food-vision-1 | 布朗尼与饼干组合 | cooking／cooking | 0.001961 |
| 2 | food-vision-1 | 懒人一锅出 | cooking／cooking | 0.001882 |
| 3 | food-vision-1 | 同一成品帧重复六次，标题写制作教程 | cooking／error（缺制作动作证据，被校验拦截） | 0.001667 |
| 4 | food-vision-2 | 真实《谁赢谁吃挑战》吃播游戏 | not_cooking／not_cooking | 0.001800 |
| 5 | food-vision-2 | 同一布朗尼视频 | cooking／cooking | 0.001870 |
| 6 | food-vision-2 | 同一一锅出视频 | uncertain／uncertain | 0.001633 |
| 合计 | | | 6次、无HTTP失败 | **0.010813** |

第3次是专门构造的负面对照，不是真实发布的视频。它暴露模型会把夹起成品当成制作，尽管应用校验阻止了误收。随后新增完全相同帧的本地拦截（不调用模型），并在V2提示词明确制作与食用区别。V2重新检查是有记录的提示词修订实验，不删除V1错误，也未重复同版本提问挑选答案。V2仍漏掉一锅出，后续需要有新次数授权的有界评估，不能将V1正例与V2负例拼成最终版本全通过。

模型部分描述包含物品数量、食材或操作细节的不准确表述。此功能只判断是否为制作内容，不能把模型描述作为可信食谱、健康效果或完整视频摘要输出。六帧抽样也不能代表完整观看。

## 视频和运行环境

两段既有正例均使用之前保存在私有目录的视频，没有新增TikHub请求。吃播视频由已保存详情返回的公开CDN地址下载，也未重查TikHub。

- 布朗尼：91.719秒；一锅出：34.100秒；吃播游戏：209.267秒。
- 新模块取六个内部时间点，最长边512像素，三列两行拼版；临时文件finally删除。
- macOS原生抽帧，两个正例各约0.75–0.80秒。
- Linux x86_64、Node v20.19.5、512MiB/1CPU、无网络的容器中，使用部署包的FFmpeg 9.0.1静态二进制：三个素材均成功，约1.78/1.79/2.39秒。这是Linux本地容器验证，**不是云函数正式运行验收**。
- 二进制共7,918,000字节，固定摘要、源代码签名和LGPL说明，见 `config/media-decoder.json` 与构建脚本。

## 账本与限制

只写视觉首验预算、视觉日账、逐次模型尝试、临时全局租约；未改线上采集配置、触发器、候选或发布快照。首验账 `vision-initial-validation` 为calls=6、allocatedMicroCny=10813、knownMicroCny=10813；失败或未知情况下仍保留全额预留的逻辑由独立行为测试覆盖，本次没有伪造未知账目。

原始视频、画面、响应、报价摘要和凭据保存在工作树忽略的 `.local/vision/`；不入Git、不进小程序。首次服务开通本身无订阅，付费仅来自本表6次模型请求。每日视觉上限0.50元及每正式轮3次不变。进一步首验调用需明确追加授权。

官方依据：
- https://cloud.tencent.com/document/api/865/131910
- https://cloud.tencent.com/document/product/865/17627

## 待验候选V3

本地已把拼图输入改为六帧独立image_url，规则版本`food-vision-3-separate-frames`；模型、预算、输出证据要求保持。目的是验证拼图缩小是否影响细节识别，目前仅是假设。各帧校验摘要并绑定请求编号，本地行为与Linux实际抽帧/请求组装已检查；真实效果未经验证，旧版本结果不能替代。用户随后明确批准追加三次和部署；实际执行结果如下。

## V3追加三次：用户授权后完成

| 样本 | 最终判定 | 按usage记账人民币 |
| --- | --- | ---: |
| 懒人一锅出 | cooking | 0.001613 |
| 布朗尼与饼干组合 | cooking | 0.001622 |
| 谁赢谁吃挑战 | not_cooking | 0.001318 |
| 追加合计 | 3/3符合预期 | **0.004553** |

范围：同一模型、同一V3规则、每例一次，无挑选性重试；新增TikHub请求0。追加账`vision-followup-20260914-v3`限3次/0.10元，通过独立映射复用既有预留/结算代码，原6次账未改；尝试编号独立前缀。两批9次共记账**0.015366元**，同日视觉账共享0.50元上限。追加适配器并发4次只准3、原账不动、重复幂等与结算测试通过，独立审查后执行；取得租约后再次确认正式轮次没有运行。

独立六帧方式改善了这两个样本的最终判定，不能从3例推断准确率。V3描述仍存在食材/数量等细节误差，程序只用于分类，不把描述发布成食谱或完整摘要。2026-09-14 18:55已部署同一受审代码，正式轮次观察另见部署记录。

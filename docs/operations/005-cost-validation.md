# 005：复用成本对照（2026-09-14）

## 结论与范围

同一批两篇内容在三小时后再次检查，使用新复用模块后，TikHub 请求从4次降至1次；详情、作者资料和文字判断重复调用从5次降至0次。两次输出的作品编号、点赞、内容判断和板块一致。这是内存数据库与固定HTTP/模型响应的离线对照，真实收费请求为0，不能推导全天费用下降75%。

| 次序 | 新搜索 | 详情 | 作者资料 | 文字模型 | TikHub总计 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 首次冷缓存 | 1 | 2 | 1 | 2 | 4 |
| 三小时后、正文和指标未变 | 1 | 0 | 0 | 0 | 1 |

两篇分别进入本周热门与低粉黑马。新搜索被明确强制执行，没有靠取消取新或丢弃候选制造省钱结果。第二次的普通详情和非临界粉丝数据仍在6小时有效期内；今日/临界数据有效期1小时，不能套用此结果。

执行：`node scripts/validate-reuse-cost.js`，退出码0；对应行为也由 `tests/content-review.test.js` 覆盖。首次对照夹具误用了完全相同标题正文，导致第一轮两个作品已经共享一次判断；改成两道不同标题的菜后重新验证，没有修改业务缓存规则。

## 生产观察仍待完成

尚未启用新发现模式。上线后需按轮记录实际请求、缓存命中、每个来源的已检查候选与入选数；与旧模式比较时同时报告时间窗口、种子与检查深度。不得把本表等同于同预算随机对照或长期产出提升。

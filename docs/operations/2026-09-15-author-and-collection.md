# 作者主页与稀疏采集修复（0.1.4）

用户已确认同一作者的A官方短链与B保留分享参数的页面入口，手机均进入正确主页、没有安全限制；随后要求直接解决今天抓取内容少。主写入者为006工作树，相关005修复同一提交交付。

## 已实施与验证

- 详情作者按钮只使用该篇已授权、已发布作品所属作者的分享信息；保留token和实际source，固定路由，不裸ID降级、不复制链接。取消无错误提示，重复点击不重复发起，授权读取失败后清空入口。
- 作者信息在既有dfp_results按作者缓存；采集仅为已入选作品补充，24小时刷新间隔、一次10秒。可选请求失败或单次执行额度用完均继续保存已验证作品；只有租约失效中止。
- 已发布作者补齐脚本经独立审查，最多15次/0.15美元，保留此前研究4次，共同计入日250/2.50美元。实际12位作者全部成功，使用12次/0.12美元，无失败，不改快照。成功可恢复缓存，未知/失败请求不自动重发；凭证错误停止批次。
- 新expanded250轮次动态保留max(12,4×min(候选数,20))次细查额度，不再因固定18/8次发现上限提前结束。轮次、含研究的日次数与金额、补跑后续轮预留均原子核对；候选数损坏拒绝收费。旧轮继续原定义。
- 稀疏队列追加未尝试关键词，图文/视频交替，保持64任务/40候选上限；新关键词持久化参与效果排序。06话题按time，最后5分钟停止发现以留细查时间。
- 视觉V4不放宽制作证据：明确合法JSON及负面证据格式，记录安全校验原因、调用编号及HTTP状态。无效输出与服务不可用分开；全部无可靠结果仍保留旧快照，不加自动付费重试。

验证命令：14个相关测试文件合计105通过/0失败/0跳过，退出0；typecheck:mini退出0；check:package退出0，12项通过、客户端25文件92348字节、解码器7918000字节。私有补齐预算3项通过，退出0。

作者改动独立复审修复了可选查询TICK_LIMIT导致最后一篇漏发，以及40秒双重试超过时间余量的问题；真实Provider内存复现现在发布全部6篇，补验通过。采集/视觉独立复审修复新关键词未入学习池、缺失候选数默认0和视觉复用丢HTTP三项；独立4个边界场景通过。

以上为源码与本地/原生Mac实验、指定A/B手机结果。实际API app_share作者在手机上的验收、0.1.4上线回读和新策略真实补跑结果，需在完成后追加，不能由这些测试替代。

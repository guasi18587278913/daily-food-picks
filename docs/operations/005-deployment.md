# 005：多入口发现与自动视觉复核上线（2026-09-14）

最新变更：19:44已将视觉上限提高到每轮20次，每日0.50元不变，源码bad4f6d。详见[更新记录](005-vision-limit-20.md)。

## 结果

用户明确批准追加三次视觉验证及部署上线。V3三例分别为两段教程cooking、一段吃播not_cooking，符合本次验收预期；追加记账0.004553元，原6次与追加3次合计0.015366元。计费依据为实际返回usage乘核验单价，并非另行核对腾讯出账明细。详见 [视觉验证](005-vision-validation.md)。

2026-09-14 18:55（上海时间）完成collectTick部署，生产代码来源 **`0d8a08d9520833be67bed9357cc48ce383f934c6`**。本次仅更新采集函数和该函数的五项配置，未上传小程序、未更改catalog/account、未推送或合并main。

## 上线证据

| 检查 | 结果 |
| --- | --- |
| 发布前 | 函数Active，正式轮次未运行，无有效租约，远离定时窗口；云端与主目录私有配置一致 |
| 部署包 | 严格视觉包检查12通过/0失败，退出码0；客户端72,305字节，Linux解码器7,918,000字节 |
| 更新代码 | `tcb fn code update collectTick`，指定此工作树函数目录，退出码0 |
| 启用配置前代码检查 | 下载实际云端包，对照26个文件逐项SHA256，含业务源、配置、依赖清单、二进制及许可证，0差异 |
| 配置 | adaptive启用、vision启用、每轮3次/每日0.50元，私有VITA密钥仅入服务器配置；主目录私有配置同步 |
| 发布后 | Active、Nodejs20.19、180秒、512MiB；再次核对26文件0差异；触发器、原TikHub预算及原快照不变 |
| 二进制权限 | 下载的实际部署zip中ffmpeg/ffprobe均保留可执行权限；与Linux静态二进制摘要匹配 |
| 未授权探针 | 一次不带服务端口令的请求，返回UNAUTHORIZED_TRIGGER |
| 合法窗口外探针 | 一次带正确服务器身份的调用，返回outside_window |
| 探针费用检查 | TikHub日账与视觉日账均未变；没有新增TikHub或视觉调用 |

部署包SHA256：`bf84f94274453a6784f71abab03827319c2cb52c9fac2d30bcdc44124bab0fa3`。回读时间为2026-09-14T10:55:11Z。

当时TikHub原日账134次，追加研究12次，真实合计146次；仍给20:00保留原池16次。配置仍为每日150次原池、06:00最多100、常规17/17/16，未把一次性研究额度变成永久自动额度。新模式发现阶段最多18/4次，包含元数据与重试。

## 独立审查与回退准备

业务源、视觉费用、图像协议和打包均完成独立审查。部署执行器另经审查，补齐远端源码/二进制内容核对、视觉独立账本核对，以及更新云端前检查本地配置漂移。

旧云端代码zip、旧配置/触发器、主目录私有配置备份、部署命令完整输出与探针结果均保存在忽略的 `.local/vision/`，不入Git。需要停视觉时将DFP_VISION_ENABLED改为false；需要回退发现策略时使用legacy。回退仍须沿用已批准边界、核对活动轮次，保留所有账本和已发布快照，不清账。

## 尚待正式轮次

下一轮为2026-09-14 20:00，次日06:00为完整今日新锐扫描。**本次完成部署和窗口外检查，不是新模式完整生产轮次的效果验收。** 旧快照没有被实验样本替换，三例视觉验证也没有导入推荐列表。T022继续记录正式轮次的请求、视觉费用、各板块新增、来源命中、缓存与未完成原因。

原001/004的指定成员真机验收、跨日观察、main合并与推送未由本次自动完成。后续从主目录部署collectTick前必须先整合本分支，避免旧工作树覆盖已上线005代码。

## 19:14额度扩容部署

用户要求现在真实补跑，并提出16次TikHub额度可以上调。本轮说明并采用常规每轮50、日250/$2.50、06:00仍100；视觉额度不变。生产代码更新为`f2b08ae9f9a6105b8fa00dafe560a4ce1ea87123`，显式`DFP_BUDGET_TIER=expanded250`才启用更高额度，旧配置与已创建的旧轮次不自动放大。新每日上限包含当天已有additionalResearch的12次，原账字段保留。常规发现最多8次、目标候选20、候选累计40及文字判断20次保持限制。

定向验证：新增预算及旧扫描/补跑38项通过；runner/发现/安全等受影响39项通过（两组存在重叠，不求和）；独立预算审查另5项通过。严格包检查12通过，均退出0。仅更新collectTick代码及日次数/金额/tier三项配置，26文件零差异；运行环境、触发器、旧快照及账本不变。部署包SHA256为`2f9978efd398729c0c40ae8cab421fa5c6ed19e3be0881c501861eb5df916c06`。窗口外和未授权探针通过，TikHub及视觉日账均未增加。

随后配置19:17临时补跑，最多50次，并为20:00保留50次；执行与结果见下一条运行记录。临时补跑不增加永久定时器，结束删除对应时间/次数配置。

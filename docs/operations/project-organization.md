# 项目目录与 SDD 整合记录

日期：2026-09-13。用户已明确授权整合规范、解决规格与源码分离，并整理目录。

## 结果与范围

唯一主目录为“美食博主工作台”。原 daily-food-picks 已成为指向主目录的兼容链接，源码不再有两份。

保留原 Git 历史、远端和 feature/wechat-member-trial 分支。迁移基线为 a42e7d6c787f；本次不修改业务逻辑、运行配置值、旧网页相对结构，不执行生产或收费操作。

整合成果保存在当前开发分支的本地提交中；没有推送或合并 main。新工作树从包含此次整合的开发分支或提交创建，才能同时取得规格与规则。

项目原则采用 1.0.0。Codex 读取 AGENTS.md，Claude Code 的 CLAUDE.md 导入同一文件；项目 SDD Skill 一份主文件、两种入口，共用 Spec Kit。

## 文件去向

| 原位置 | 当前位置 |
| --- | --- |
| 相邻 daily-food-picks 的源码和 Git | 当前项目根，原分支和历史保留 |
| 根目录开发规范 | `docs/standards/` |
| 原 docs/wechat-trial-runbook.md | `docs/operations/wechat-trial-runbook.md` |
| 根目录十份早期研究 Markdown | `docs/archive/collection-research/` |
| 原采集验证、参考资料 | 同一历史归档组的原目录名，默认本机保留 |
| 小程序头像与生成说明 | `assets/branding/` |
| 二维码与旧预览 | `.local/previews/` |
| 浏览器操作日志 | `.local/browser-history/` |
| 完整账号 JSON 与报名原文 | `.local/accounts/001-wechat-member-trial/`，规格原位置保留脱敏摘要 |
| 两目录完整备份与迁移脚本 | `.local/organization-backups/20260913/` |

## 完整性依据

备份在移动前完成，包含两目录原始材料和 Git 元数据；依赖目录 node_modules 原样搬迁，没有重复备份。

迁移清单记录了 2,105 个原文件。首次迁移后核对：2,101 个内容完全一致；4 个预定变化为两份忽略规则合并、项目原则填写和本地功能指针。没有缺失、未解释变化或文件权限变化，退出码 0。

随后有意修订的内容只包括：当前文档路径和导航、规范接入状态、001 功能过期状态说明、运行记录账号标识脱敏。完整原文仍在备份；历史研究和原始响应保持原样。

私有目录中保存 `manifest.json` 和 `move-map.json`，记录原根、原路径、新路径、摘要和权限。它们不进入 Git。

## 审查与验证

迁移前独立审查发现两份原始账号记录会进入默认待提交范围；已改为私有保存、共享脱敏摘要，并增加兜底忽略。复审通过，7 项通过、0 失败，退出码 0。

最终验证完成，以下命令和检查均退出码 0：

| 验证 | 实际结果 |
| --- | --- |
| 最终文件摘要 | 2,105 条原文件映射全部存在；2,089 内容未变，16 条为已声明的文档／忽略规则修订 |
| 独立迁移结果审查 | PASS；文件权限、Git 历史、分支、远端和原业务勾选保持 |
| 当前文档链接 | 119 个本地链接无断链 |
| 私有边界 | 13 项忽略命中；142 份可入库文本检查未发现已知受保护值；私有路径未被跟踪 |
| 新旧入口与功能解析 | 4 项路径检查通过；恢复 001 后完整前置检查通过 |
| 共享 Skill | 官方校验通过；Claude 同源链接与共同入口一致 |
| 独立前向场景 | 讨论作者筛选、继续 001、换 Claude 接手三种场景均正确分流 |
| 生成副本与包检查 | 4 份副本生成；6 项包检查通过、0 失败，客户端 12 文件／35,466 字节 |
| 小程序类型检查 | 0 错误 |
| 旧网页只读检查 | 1 轮、34 篇数据与图片引用通过，index.html 未重建 |

业务源码与配置值未变化，因此没有重复运行全量业务测试，也没有真实取数或云端验收。前向检查验证了文件与接续路线，没有新开真实 Codex／Claude Code 会话验证当前界面的自动加载；使用时从主目录新开会话。

另建独立工作树补测首次接手：它不包含本地功能指针，未指定功能时返回退出码 1，符合缺少上下文的预期。显式选择 001 后定位通过、退出码 0，且没有创建默认指针。移除工具缓存后，四种核心模板仍可解析，均退出码 0。项目 Skill 已补充无指针时的新功能／已有任务分流。

独立读者在该工作树再次验证“讨论新需求”和“继续指定功能”：两项 PASS，33 个本地引用无断链，入口同源，验证前后状态未变。临时工作树在验证结束后清理，不保留额外开发副本。

Spec Kit 的集成目录缓存和模板摘要元数据只保留本地，已从默认版本控制中排除；真正的模板、双工具适配和工作流配置继续共享。

## 接续业务功能

本次整理使用 `specs/002-project-foundation/` 留存需求和任务。已恢复原业务功能指针 `specs/001-wechat-member-trial/`，并实际通过前置检查，便于继续指定成员试用验收。

后续显式选择其他功能时使用其目录；不要按 Git 分支名推断 Spec Kit 的当前功能。并行任务不要争抢同一工作区指针。

原业务中尚未完成的指定成员身份绑定与手机验收仍留在 001 的 tasks，本次整理不将它们标为完成。

## 恢复与兼容

旧英文源码入口保持可访问，指向同一份项目。重新打开开发工具时优先选择主目录；已有会话的技能列表需刷新或新开会话核对。

如需撤销目录迁移，先停止本地写入，对照迁移清单把文件移回；恢复两个原始根及各自忽略规则。不能用无差别删除覆盖迁移后新产生的工作。

恢复资料从本机备份读取，不从线上重新采集，不清预算、不更改云端配置。归档历史脚本仍保持历史环境假设，不能当作当前自动任务直接运行。

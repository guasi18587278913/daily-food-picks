# 006 原文跳转本地实现交接

工作树：美食博主工作台-note-navigation，分支feature/note-navigation，基线f18a1a2。主写入者为本会话。**基础接线已完成，尚未更新云端、上传体验版或登记任何生产链接。批量获取链接和真实绑定仍未完成，不能声明所有选题已可直达。**

**9月15日最新结果：** 4篇已存图文有微信分享path；两篇已推荐视频通过图文详情接口也取得对应path，花费0.02美元计入日总额。但用户已确认测试的2图文+2视频全部只到首页，正文直达0成功/4失败；这批首页中转路径不登记或上线。字段获取是已验证事实，实际可用仍未解决。已准备直接打开返回值内层笔记页的两篇对照，保留全部原始查询参数、不再付费取数，待真机确认。详见[最新验证记录](2026-09-15-image-navigation-check.md)。006候选代码仍只支持shortLink，尚未接入未经正文验证的path。

## 本地完成

- shared/source-navigation.js作为共享校验源，生成到前端和catalog；新工作树先执行npm run prepare:functions，不能手改副本。
- 单个服务端入口目录最多100条、一次导入最多20条，按版本事务合并；拒绝未确认、未发布笔记、重复短码指向不同笔记和过期版本。
- catalog只在返回非空笔记时读目录一次，只下发返回笔记的链接；状态轮询、轮次列表、空结果及拒绝请求不增读量。目录故障保留数据与封面；来源复制已按用户要求移除。
- 按用户最新要求移除来源复制：有有效绑定时只显示查看原笔记，无绑定显示非交互的原文暂不可直达。按原样shortLink发起，不猜path；取消静默，失败仅提示，不操作剪贴板，不把API成功当正文已展示。
- 独立审查发现并修复刷新竞态：用户点击旧“查看”按钮而描述刚被撤销时，不再自动复制；描述撤销走确认，笔记已移除只提示刷新。

## 验证

- node --test tests/source-navigation.test.js tests/catalog-navigation.test.js tests/source-action.test.js tests/source-page.test.js tests/catalog.test.js tests/frontend.test.js：55通过/0失败/0跳过，退出码0。
- npm run typecheck:mini：退出码0。首次因新工作树缺类型依赖失败，复用原项目已安装node_modules后检查通过，没有修改依赖版本。
- npm run check:package:vision：12项通过，退出码0；小程序19文件/79,805字节。
- 模拟器390×753实际渲染本地样例：查看按钮114×45px，复制按钮60×45px；无链接时一个复制按钮。云调用已拦截，因此这只是布局验收，不是真实账号/笔记验收。
- 服务端独立审查6项通过；前端独立复审确认刷新竞态修复。没有运行额外付费探活，也没有重复全量CI；仓库仍无统一CI。

## 未完成与下一步

1. 用户提供过可正常打开的视频官方小程序短链接，但尚缺普通分享链接/笔记ID；用户要求助手自行补充，不再把请求用户逐篇复制作为默认下一步。标题作者只能辅助定位，不能未经确认就绑定。若样本不在工作台已发布笔记中，不能把它强行插入榜单。
2. 现有采集没有官方shortLink字段，公开TikHub文档未找到明确生成端点。网页URL和QQ分享路径不能直接当微信短链；自动批量来源仍需实际验证。运维登记工具不是自动取链能力，不能要求用户逐篇复制来冒充全自动完成。
3. 对应关系确定后，先用scripts/register-note-links.js --input 私有JSON文件做只读预览；生产登记还需明确核对。脚本默认不写，--apply才写目录。
4. 真实数据准备好后，更新候选catalog/体验版，再从正式卡片验证目标内容。此前测试页单篇视频成功不能替代这一联动验收。

当前线上采集保持bad4f6d，视觉20次/轮、日0.50元；TikHub日250次。此分支不需要部署collectTick。封面缺失及20:00自动轮次观察属于此前独立待办，本次未冒充完成。

## 用户取消复制来源后的更新

卡片复制按钮、失败复制选项、onCopy/copySource均已移除；管理员邀请码复制保留。原复制成功用例随功能撤销移除，替换为跳转失败/撤销时零剪贴板写入的行为检查。40项受影响测试通过，类型检查退出0，独立复审通过。模拟器390×753：有入口时仅1个114×45px查看按钮，无入口时来源按钮数0，仅状态文字。包检查12项通过，客户端78,085字节。此更新仍为本地待发布版本，生产绑定与批量来源依赖未消失。

## 2026-09-14 自行补充调查

- 使用已有浏览器账号完成 CloudBase CLI 网页授权，未退出或切换账号。随后只读查询 source_navigation_v1 成功（退出0）：目录不存在，登记数0。没有写生产数据。
- 私有官方短链测试页经实际点击开发者工具的“允许”后，回调仅返回 navigateToMiniProgram:ok，没有笔记ID或目标path。此结果只表示开发工具接受跳转，不能补充目标身份或代替真机正文验收。
- 重新读取 TikHub 当前公开 OpenAPI（Node fetch HTTP200、退出0），未找到明确微信小程序短链生成或解析端点。Python读取曾HTTP403，未作为业务接口调用。
- 检查6份既有视频详情响应文件，共18个 qq_mini_program_info 对象，未发现官方微信短链；未新增 TikHub 或其他服务付费请求。该样本范围不能推出所有接口均无此能力。
- 尝试通过本机微信正常窗口操作核实内容，未获得可读取的笔记页面；没有读取聊天、发送消息或使用第三方协议账号。
- 结论：T007/T009/T010仍未完成。已知单篇短链可用，但尚无可靠自动取链/反查来源，因此本轮未发布一个实际可点击链接为0的版本。代码验证仍对应246ceac，本轮仅补调查记录，未重复代码测试。

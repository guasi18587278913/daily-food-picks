# 三条工作线收尾记录

日期：2026-09-14，北京时间。主写入者：本轮 Codex；独立审查者只读检查。

**当前结果**：云端账号、0.1.2体验版、名单迁移与关闭已完成；视频制作意图和候选检查机会优化已于12:17部署，验证见[采集策略运行记录](2026-09-14-collection-strategy.md)，效果等待后续正式轮次。累计榜暂不调整。大芙真机验收、main合并与推送仍未完成。下文保留各阶段当时的事实。

## 源码与线上状态

本地整合工作树：`美食博主工作台-closeout`，分支 `integration/2026-09-14-closeout`。原三个工作树保持原分支，未合并 main、未推送、未部署。

| 工作线 | 已核对源码 | 线上证据与未完成项 |
| --- | --- | --- |
| 001 06:00 扫描 | `feature/today-hot-sweep` / `234a5c9` | 9/13 部署的实现是 `2afbedb`；9/14 06:00、09:00 观察在原运行记录。需完成有结果时的跨轮沿用验收 |
| 003 登录、角色与云收藏 | `feature/wechat-login-roles` / `cd8722c` | account、catalog 已部署；前端上传、管理员引导、名单迁移及关闭窗口未完成 |
| 004 新界面 | `004-ui-visual-refresh` / `50a223f` | 代码已提交；体验版与指定成员真机验收未完成 |

本地先将 sweep 合入登录代码，再合新界面；两个整合提交为 `83597f6`、`8b4fde2`。首页的三个冲突逐项解决，保留服务端登录状态、管理员入口、云收藏提示、状态颜色与新卡片样式。后续采集和收藏修复在同一分支接续，最终提交以本记录的验证补充为准。

最终修复源码已固化为 `f1d22a5`。本记录中的99项定向行为测试、类型检查、包检查、调度检查和独立复审均对应其源码；后续仅补充本文提交标识，没有再改业务代码。

## 已处理的问题

- 请求账本新增经过数值校验的 `httpStatus`、`providerCode`、`providerDataCode`，不保存响应正文、令牌或错误消息。网络失败保留未知状态；重试次数与预算不变。旧请求没有状态码，不能回补猜测。
- 有成功搜索却没有入选作品时，可以发布说明覆盖缺口的 `partial` 零结果，并执行当天 06:00 作品的沿用。搜索全部失败，或授权／模型故障且没有有效作品时仍保留旧快照。
- 首页文案说明每天 06:00 筛选前 24 小时的今日新锐；更新时刻包含 06:00、09:00、12:00、20:00。
- 独立审查复现并修复 003 旧问题：一台设备取消后，另一台设备会把已同步旧副本重新合并到云端。现在本机 v2 将副本与尚未上传新增项放在一次本地写入中，只上传后者；保留旧 v1 作为首次升级来源。断网新增与同步期间新增均可在冷启动后恢复；已同步收藏的取消需要连接，失败时明确提示。
- 后续独立审查将收藏问题定位到两个方向的交错：合并中取消会被回复恢复，取消在途时同步也可能读到旧值。现已用同一执行队列串行完成云端写入和同步，合并重复同步请求，并对同步期间的冲突操作明确提示完成后重试；不再假报取消成功。
- 修正过时的“尚未部署／尚未核对配额”和“旧名单已停用”交接文案。临时身份捕捉开关已在 003 上线时移除，大芙不再需要重新启用该开关。

## 待确认的产品口径

本轮已向用户提出下列两个选择，尚未收到答复，因此未改变对应规则：

1. 视频判断：建议标题或正文明确表达制作方法的视频可入选，纯菜名仍为证据不足；不增加视频下载或转录。原有正文证据标准继续运行。
2. 本周热门：建议改为近 7 天累计榜，并优先检查本周候选；需要同步修改跨轮去重的展示例外及快照验收。现有每轮新增规则和候选排序暂未变更。

每日 150 次／1.50 美元、06:00 100 次、常规轮 17/17/16 次均保持现有授权。55 词不限类型的扫描仍可能漏掉图文；若调整视频／图文搜索配比，须先明确在现有额度内怎样分配。轮询聚合与退避仍由 003 T070 跟踪，未据此开放 100 用户。

## 云端只读核对

9/14 当前 CLI 登录有效，但 `env list` 只返回另一个项目的环境；美食环境的账单查询和临时凭据数据库查询均返回环境不属于当前账号／`INVALID_ENV`。已检查上海地域，结果相同。未退出登录、切换账号、改权限或发起采集。

因此本轮没有取得新的美食环境用量、用户记录或轮次数据。最近可靠证据仍是：9/13 23:10 配额回读和9/14 09:58 采集观察，见[多用户运行记录](multi-user-access-runbook.md)与[试用运行记录](wechat-trial-runbook.md)。不能将历史用量写成当前用量。

TikHub 官方[账号信息接口](https://docs.tikhub.io/186826050e0)提供余额字段，但尚未核实本次调用的零费用口径；没有发送携带密钥的请求。浏览器工具没有已连接浏览器，余额未回读，也未充值。9/13 20:00 被拒的具体状态仍未知。

本地 `.local/closeout/read-cloud.js` 已备好一次有界的只读查询：读取状态、最多12个轮次、账本和用户数摘要；不调采集、不返回用户身份、不保存凭据。恢复正确 CloudBase 登录后可运行并写入观察证据。

## 上线与验收顺序

1. 收到产品口径后完成对应实现、受影响验证与独立复审，将待交付内容固定在同一提交。
2. 按已确认方案合并 main、推送；当前仓库没有统一 CI，合并前完整检查的执行方式须落实，不能把本地定向检查冒充 CI。旧自动推送钩子在本轮本地提交时被单次禁用，未修改其安装配置。
3. 恢复有美食环境权限的登录，回读当前函数状态、预算及用户记录；从同一整合目录部署 collectTick 修复并上传一次小程序体验版。不要从未整合的旧工作树部署。
4. 维护账号在手机打开新版，确认管理员建立和管理入口；再执行 `admin.migrateWhitelist`，记录 `{created, skipped, total}`，回读原名单已全部迁移。
5. 确认迁移无遗漏后，关闭 `DFP_MIGRATION_FALLBACK` 并清空 `DFP_ALLOWED_OPENIDS`，部署 account/catalog、复核已迁移用户仍能访问。不得跳过第4步提前清空。
6. 大芙使用管理员提供的邀请码开通，合并执行下面的验收。未自动联系或给任何人发码。

部署、生产迁移与权限关闭需要对应明确授权；本轮尚未执行。现有迁移窗口保留，避免旧版用户掉线。

| 验收组 | 具体项目 | 当前证据 |
| --- | --- | --- |
| 001 + 004 指定成员 | 进入、看到更新、搜索、排序、收藏后重开、复制来源 | 待大芙手机；原 T032 与 T022 保持未完成 |
| 003 账号与角色 | 管理员引导、生成码、未开通拒绝、开通、码用尽拒绝、停用恢复、跨设备收藏、最后管理员保护 | 待真机；T073–T076 保持未完成 |
| 界面补充 | 375×667、320与430宽度实际截图，安卓图标 | 尚未覆盖；当前模拟器为390×753 |
| 采集观察 | 今天12:00与20:00、明天06:00与09:00；请求数、费用、板块新增、失败原因、是否沿用 | 记录时尚未到这些轮次；不补造触发或虚构观察 |

## 验证记录

以下均为本机 Node v22.22.2 的离线检查，没有付费数据或生产模型调用。

| 检查 | 结果 |
| --- | --- |
| 新增诊断与零结果回归，修复前 | 退出码1；27项中21通过、6失败，失败均命中预期行为 |
| 采集修复后：provider-diagnostics、daily-sweep、runner、safety、publishing | 退出码0；52通过、0失败、0跳过 |
| 收藏新回归，修复前 | 退出码1；31项中28通过、3失败；同步中取消及反向顺序各有1项行为失败 |
| 收藏修复后：frontend、favorites | 退出码0；47通过、0失败、0跳过，涵盖两个方向的交错、断网新增与云端取消 |
| 前端类型检查 | 退出码0 |
| 模拟器 | 390×753，基础库3.17.2；邀请码、确认中、停用、阻止、管理员首页、普通用户无管理入口、复制弹窗7项完成；7类可点击控件最小45px |
| 独立审查 | 已修审查发现的问题，最终复审无阻塞；独立补验两个方向的交错和写入／同步失败恢复共4通过、0失败、0跳过，退出码0 |

模拟器在 `.local/closeout/simulator-project` 的本地副本中运行，加载页面之前拦截全部云调用，不初始化生产云环境。首次测量误将37px的“往期”内层胶囊当作点击控件，改测实际picker后为45px；未修改产品来降低点击尺寸门槛。最后一次收藏执行顺序修改不涉及WXML/WXSS，界面截图沿用，行为以最新47项测试为准。

完整测试、生产构建、CI、真机与上线均未执行。只有实际完成后才回写对应任务。

部署副本检查：8份生成副本更新；`npm run check:package` 退出码0、10通过、0失败，17个客户端文件共72,305字节。`node scripts/check-schedule.js` 退出码0、128通过、0失败。差异自审通过，无新增循环依赖、凭据、原始响应或手改生成副本。规则版本更新为 `2026-09-14-partial-zero-results`，只标记本次发布行为变化，不改变门槛或额度。

收尾环境：本轮只关闭本地模拟器验证副本，其他工作区不变；微信开发者工具安装盘已推出，服务端口保留。验证依赖曾临时链接主工作区的 node_modules，提交前移除该链接；再次运行本地验证前需准备依赖。

## 10:42以后：已授权上传与名单迁移

源码为`c63cc69`（业务代码仍为`f1d22a5`）；未合并main、未推送。本次只更新文档，无业务代码改动。

1. 发布前执行一次`npm test`，退出码0，199通过、0失败、0跳过。本批次是本地完整测试，不称CI。
2. 微信开发者工具CLI能列出正确的美食环境，account/catalog/collectTick均为Active，运行时Nodejs20.19，超时分别30/30/180秒。通用CloudBase CLI仍是另一个项目的登录；`tcb login --flow web`只返回已登录，没有切换或退出原账号。
3. 10:46通过真实微信调用`account.me`，维护身份被服务器已有配置引导为active/admin；管理列表中1个用户、1个active admin。没有伪造OPENID，也没有直接写数据库来绕过授权入口。这是开发工具中的真实云调用，不替代手机验收。
4. 微信开发者工具`upload`成功上传0.1.2，说明为“微信登录、邀请码与新界面；修复跨设备收藏同步”；退出码0，平台实际包体78,369字节。这里只证明开发版本已上传，尚未证明设置为体验版。
5. 10:49执行`admin.migrateWhitelist`，返回`{created:0,skipped:1,total:1}`。唯一旧名单身份已在前一步建为admin，所以被正常跳过；随后逐项比对原名单与服务端用户记录，1/1匹配，active admin仍为1。
6. 10:54迁移后真实复验：account.me仍为admin，catalog.getRound成功返回2条低粉黑马。当前采集状态仍为9/14 09:00预算耗尽，最近可用快照仍是9/13 12:00；未把此旧快照说成新采集结果。
7. 独立只读复审确认上述管理员、迁移和访问证据成立。关闭窗口前还需比对account与catalog各自云端有效名单，不能只依赖本地account配置；只记录人数和一致性，不公开身份。

私有执行证据在`.local/release-20260914/`：上传信息、管理员响应、迁移摘要及后续读取摘要均已脱敏；配置备份、二维码和授权日志另存于同一被Git忽略的私有目录，只留本机、限制文件权限。TikHub余额沿用用户确认，未再发余额请求，未发起采集或模型付费探活。

其中`upload-execution.json`记录本会话真实CLI上传命令的版本、源码、退出码与成功结果；`upload-info.json`只含平台回报包大小，不能单独证明体验版已设置。

接续事项（最新状态）：

- 已完成：用户接通Chrome并扫码后，微信后台回读0.1.2已是体验版。没有把预览码当体验码。
- 已完成：回读两端实际名单并核对已迁移后，移除`DFP_ALLOWED_OPENIDS`并将`DFP_MIGRATION_FALLBACK`设为false；其他变量保留，云端接口与本地配置均已核对。
- 大芙及未授权账号手机验收、各屏幕尺寸与安卓图标。指定成员尚未在应用侧开通，原名单只有维护者；待新体验版可用后由管理员发邀请码，不重开旧身份捕捉开关。
- 采集函数本轮未重新部署，线上仍为原sweep实现；HTTP诊断与零结果发布修复还只是本地版本。

## 11:00以后：浏览器接入、体验版确认和迁移关闭

用户开启Chrome远程调试并完成微信扫码登录。复用已有本地CDP Proxy访问后台；本次只使用和关闭本任务创建的标签页，原有标签页保留。

- **正确云账号**：浏览器已登录美食环境；通用CloudBase CLI旧缓存先保存在用户配置目录的私有备份中，未撤销旧项目授权，再按正常网页流程重新授权当前账号。11:19执行`tcb env list --json`退出码0，只返回正确的美食环境、状态NORMAL；旧“环境不属于当前账号”问题已解决。
- **体验版**：微信后台版本管理页显示0.1.2、上传时间10:48:06、备注与上传命令一致，且该版本已带“体验版”标记和“取消体验”菜单。这里回读确认现有体验指向，无需重复上传或取消再设置。未提交正式审核或发布公开版本。证据为`trial-confirmed.json`与`trial-version-012.png`。
- **关闭前核对**：从account与catalog各自云控制台实际显示的环境变量读取并私下比对，两端名单各1人、完全相同，且与已迁移名单匹配；只在摘要记录人数和一致性，不公开身份。证据为`migration-cloud-precheck.json`。
- **关闭配置**：两端`DFP_MIGRATION_FALLBACK=false`；控制台不接受空的环境变量值，因此移除`DFP_ALLOWED_OPENIDS`整项，效果为清空旧名单。其余变量保持原值。account与catalog保存时间分别为11:12:51、11:13:53，随后刷新确认状态正常。
- **接口回读**：11:21通过重新登录的CLI取得临时凭据，由SCF `GetFunction`独立回读，两端均Active、Nodejs20.19、30秒、256MB，旧名单变量不存在、回落关闭、其他变量与关闭前相同。验证脚本退出码0，见`function-config-readback.json`。
- **实际访问**：11:17关闭后再次使用真实微信上下文调用，account.me仍为admin，catalog.getRound成功返回2条低粉黑马；退出码0，见`catalog-after-close.json`。这是云端实测，仍不能替代大芙手机或未授权微信账号验收。
- **本地配置**：主工作区私有配置同步移除两端旧名单并关闭回落，避免下次部署重新打开。改前私有备份保留，未改collectTick任何配置，未将私有文件加入Git。
- **独立复核**：审查者只读核对脱敏回读、脚本和截图，确认账号对齐、体验版确认和迁移关闭证据完整，无阻塞项；没有把开发工具云调用当手机验收。

### 当前运行与用量

11:19只读核对：9/14程序账本仍85次、0.85美元；06:00轮68次、09:00轮17次，均无新增入选。最近可用快照仍是9/13 12:00的2条低粉黑马。未补造触发、未清额度、未把旧快照当成新采集。

云资源回读：个人版，资源生命周期为9/12至10/12，套餐有效期至2027-03-12；`EnableOverrun=true`、用量状态正常，未改变续费或超额设置。控制台用量表明确分列：截止昨日本周期调用4788次，本日6648次，套餐调用额度20万次；未把“截止昨日”写成实时累计。该口径仍不能直接替代此前按数据库读／函数调用拆分的估算。未据此开放100用户。

### 仍待完成

大芙需要管理员提供的邀请码开通应用，再完成001/004六项真机操作与003账号、跨设备收藏验收；小屏、大屏和安卓图标尚未验。视频判断、累计榜与本周检查顺序仍未改变。采集函数修复未部署，main未合并、分支未推送。未来轮次观察仍按真实时刻记录。

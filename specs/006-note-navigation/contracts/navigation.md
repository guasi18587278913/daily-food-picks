# Contract

- 仅接收#小程序://小红书/...格式的官方短链接；字符串校验不能证明目标，核对声明属于受信运维输入，不采信笔记正文。
- 同一短码不能关联两个笔记编号。目录版本控制防止并发覆盖。
- 仅已发布笔记可注册入口，不把用户提供的任意样本插入推荐榜。
- catalog遵循原调用者授权，再读取一次目录；外部付费调用数为零。旧notes字段和值保持。
- 客户端再次核对noteId及链接形状，点击时原样调用shortLink，不传猜测path。cancel不触发复制或报错；其他失败需用户选择复制。
- 无有效入口仅显示复制链接；目录失败不丢正文、指标、封面。

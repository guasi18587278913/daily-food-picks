# 美食选题工作台：共同开发入口

本文件由 Codex 和 Claude Code 共用。Claude Code 通过根目录 CLAUDE.md 导入。

## 项目位置

- 本目录是唯一项目根：源码、`.specify/`、`specs/` 和 `docs/` 均在这里。
- 原 `daily-food-picks` 路径是兼容链接，不是另一份仓库；不要复制出第二套代码或规格。
- 当前产品是原生微信小程序；代码在 `miniprogram/`、`cloudfunctions/`。旧网页保留根目录运行结构。
- 历史研究在 `docs/archive/collection-research/`，只作溯源；原始响应、试跑和 `.local/` 不作为当前需求。

## 每个开发任务的入口

收到新增功能、继续开发、修复或重构请求后，在首次修改业务代码前，先读取并执行项目 [food-picks-sdd](.agents/skills/food-picks-sdd/SKILL.md)。确认本次功能、当前阶段、对应任务或小改范围与验收方式，用一句话说明后按已有授权继续。

同一任务的后续轮次复用已核对的材料；目标、材料或代码状态变化时重新核对。新任务重新确认入口，不能未经核对沿用上一任务的功能选择。只讨论或只审查时保持用户要求的只读范围。

## 开发前读取

1. [项目长期原则](.specify/memory/constitution.md)。
2. [风险与规范入口](docs/standards/README.md)，再按任务读对应细则。
3. 按上述开发入口接续既有 Spec Kit；跨工具交接与文档分工见 [SDD 工作流](docs/workflow.md)。
4. 当前功能以用户明确指定为先，否则读取 `.specify/feature.json`；新工作树可能没有该指针，按工作流识别新功能或已有任务，再核对材料与 Git 状态。

只读讨论和资料整理按实际目标执行，不因读到 Skill 示例就自动改代码。当前明确用户指令和已获授权优先，不重复申请同一许可。

## 必须保持

- 新功能先定需求和验收；继续任务先读已有材料，不重写已完成的方案。
- 单一职责、明确依赖、禁止新增循环依赖，命名说明业务含义和副作用。
- 关键逻辑有行为测试；不吞错误、不造假成功、不削弱测试凑通过。
- 开发中只验证受影响范围；高风险按规范确认和独立审查，不让小改背全量流程。
- 当前请求身份、读取与采集分离、预算预留、失败保留旧快照是项目底线。
- 不把原始响应、密钥、二维码和私有日志加入 Git，不运行历史付费脚本来验证目录。
- 不因“测试”获得生产或付费授权；不自动推送、合并或部署。

## 双工具接续

- 一个任务有一个主要写入者；另一方可审查。同一文件和任务状态不要无协调地同时改。
- 并行功能使用独立工作树或明确文件范围。`.specify/feature.json` 是本工作区指针，不由多个任务抢写。
- 交接写清任务编号、源码状态、完成项、验证证据、剩余项和下一步；细则与记录格式见工作流。
- 已有 Spec Kit 适配保留在 `.agents/skills/speckit-*` 与 `.claude/skills/speckit-*`。不要复制项目规范进去。

## 验证入口

- 后端共享源或配置变化后：`npm run prepare:functions` 刷新生成副本；不要直接修改副本。
- 测试按 [测试与交付](docs/standards/测试与交付.md)选文件；前端用 `npm run typecheck:mini`。
- 部署包结构用 `npm run check:package`。`npm test` 只覆盖当前小程序模块，不能冒充真机或生产验收。
- 完成结论必须有实际结果：退出码、通过／失败／跳过，及对应源码与环境。

## 沟通

用中文，结论先行，说人话。技术词第一次出现时解释用途；不直接堆报错、字段和提交编号。
工作中说明发现和下一步；最终给结果、必要证据和实际限制。
每次最终回复末尾附一行 `<!-- 通知摘要：我已经……。 -->`，用 15–35 字的一句话说明进展，不写命令、文件名或测试细节。
